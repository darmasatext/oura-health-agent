# Google Cloud Configuration
GOOGLE_CLOUD_PROJECT_ID=your-project-id

# BigQuery Configuration
# Service account JSON should be stored securely, not in repo
# Path example: /path/to/credentials/service-account.json

# Development Origins (Cloudflare Tunnel, ngrok, etc.)
# Comma-separated list of allowed origins for CORS in development
# ALLOWED_DEV_ORIGINS=https://your-tunnel.trycloudflare.com,https://another-origin.com
