steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/${_JOB_NAME}:latest'
      - '-f'
      - 'Dockerfile.v2'
      - '.'
images:
  - 'gcr.io/$PROJECT_ID/${_JOB_NAME}:latest'
